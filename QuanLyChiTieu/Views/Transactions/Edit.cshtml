@model QuanLyChiTieu.Models.Transaction

@{
    ViewData["Title"] = "Chỉnh sửa Giao dịch";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0" style="border-radius: 20px;">
                <div class="card-header bg-warning text-white text-center py-3" style="border-radius: 20px 20px 0 0;">
                    <h4 class="mb-0 fw-bold">✏️ Chỉnh sửa Giao Dịch</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit" asp-route-id="@Model.TransactionId" method="post">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <input type="hidden" asp-for="TransactionId" />

                        <div class="mb-3">
                            <label class="form-label fw-bold">💳 Chọn Ví</label>
                            <select asp-for="WalletId" class="form-select border-warning" asp-items="ViewBag.WalletId">
                                <option value="">-- Chọn ví của bạn --</option>
                            </select>
                            <span asp-validation-for="WalletId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">📂 Danh mục</label>
                            <select asp-for="CategoryId" class="form-select border-warning" asp-items="ViewBag.CategoryId">
                                <option value="">-- Chi tiêu vào việc gì? --</option>
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">💰 Số tiền (VNĐ)</label>
                            <input asp-for="Amount" class="form-control border-warning" type="number" placeholder="Ví dụ: 50000" required />
                            <span asp-validation-for="Amount" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">📅 Ngày giao dịch</label>
                            <input asp-for="TransactionDate" class="form-control border-warning" type="date" value="@Model.TransactionDate?.ToString("yyyy-MM-dd")" />
                            <span asp-validation-for="TransactionDate" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">📝 Ghi chú</label>
                            <textarea asp-for="Note" class="form-control border-warning" rows="2" placeholder="Ăn sáng, đổ xăng..."></textarea>
                            <span asp-validation-for="Note" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning btn-lg fw-bold shadow-sm">Lưu thay đổi</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Quay lại danh sách</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}